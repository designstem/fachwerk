// computed: {
//   points() {
//     return Array.from({
//       length: this.count
//     }).map((p, i) => ({
//       x: cx((360 / this.count) * i, this.radius),
//       y: cy((360 / this.count) * i, this.radius)
//     }));
//   }
// }
